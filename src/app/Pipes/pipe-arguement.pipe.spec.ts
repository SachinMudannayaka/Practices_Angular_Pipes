import { PipeArguementPipe } from './pipe-arguement.pipe';

describe('PipeArguementPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeArguementPipe();
    expect(pipe).toBeTruthy();
  });
});
